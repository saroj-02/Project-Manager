{
  "name": "project-manager-root",
  "version": "1.0.0",
  "description": "Root configuration for automated Render deployment",
  "scripts": {
    "install-client": "npm install --prefix client",
    "build-client": "npm run build --prefix client",
    "install-server": "npm install --prefix server",
    "build": "npm run install-client && npm run build-client && rm -rf dist && cp -r client/dist dist",
    "start": "npm run install-server && node server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
